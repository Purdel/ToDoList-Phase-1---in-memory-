[tool.poetry]
name = "todo-list-phase-1"
version = "0.1.0"
authors = ["Pordel"]
readme = "README.md"
packages = [{include = "hello_poetry"}]

[tool.poetry.dependencies]
python = "^3.12"
python-dotenv = "^1.1.1"

